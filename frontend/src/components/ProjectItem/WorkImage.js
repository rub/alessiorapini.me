import React from "react"
import { StaticImage } from "gatsby-plugin-image"

export default WorkImage = ({ url, title }) => {
  return (
    // TODO: Update alt dynamically with the work title
    // <StaticImage src={url} alt={title} />
    // <img src={url} alt={title} />
    <div />
  )
}
